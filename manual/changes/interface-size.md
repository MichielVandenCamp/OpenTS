---
title: Size the interface apart from the resolution
category: feature
release: 0.2.0
targets:
- type: key
  id: UIHeight
  effect: added
credit: [Michiel Van den Camp]
---

The display options screen has an interface size, stored as `UIHeight`, that sets how large the sidebar, tab strip, radar, menus, and dialogs are drawn whatever the resolution.
